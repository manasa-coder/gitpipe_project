pipeline{
		agent any
				stages{
						stage(build) {
									steps{
											echo 'this is git and pipeline sample test'
										}
									}
					    stage(deploy) {
									steps{
										     echo 'this is git and pipeline sample test'	
									}
						}
					}
}
